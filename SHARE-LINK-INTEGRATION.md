# Share link

The UI button copies a direct Vercel vehicle URL:

`https://japanautoimportui.vercel.app/vehicles/{carId}`

No redirect or configuration on `japanauto.bg` is required.

Button label:
`Копиране на линк към автомобила`
